{
	"name": "joot",
	"version": "0.1.0",
	"scripts": {
		"build:clipboard": "cd clipboard && cargo build -r",
		"build:crx": "pnpm -F background build && pnpm -F popup build && cp assets/* bundle/",
		"pack": "rm -rf bundle && pnpm run build:crx && pnpm run build:clipboard"
	},
	"devDependencies": {
		"@hyrious/esbuild-dev": "^0.10.5",
		"@jest/globals": "^29.7.0",
		"@types/chrome": "^0.0.266",
		"@types/jest": "^29.5.12",
		"@types/node": "^20.12.7",
		"esbuild": "^0.20.2",
		"jest": "^29.7.0",
		"ts-jest": "^29.1.2",
		"typescript": "^5.4.4",
		"vite": "^5.2.9"
	}
}